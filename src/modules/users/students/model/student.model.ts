export interface iFormStudent {
  name: string,
  email: string,
  phone: string,
  date_of_birth: string|null,
  age: string,
  weight: number | null,
  height: number | null,
  access: number,
  id: number | null
}
